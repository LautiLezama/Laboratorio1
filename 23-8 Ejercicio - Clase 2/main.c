#include <stdio.h>
#include <stdlib.h>

int main()
{
    /* El sistema operativo es quien administra el uso de memoria de RAM del programa.

Build = Compilaciòn = Crea el programa ejecutar y traduce el codigo a binario.

Linkeador = Relaciona funciones con bibliotecas.

#include <tipo de funciones> habilita funciones para el codigo.

#include <math.h> = funciones matematicas
#include <conio.h> = getch/getche
#include <ctype.h> = toLower(var); toUpper(var);

system("pause"); para pausar el ejecutable.

\t = tab
\n = space
\a = sonidito xq salta la mother

source code formatter = corrector de linea.

Buffer = memoria intermedia que almacena los caracteres antes de asignarlos en RAM (deja \n cuando usa otro scanf).

fflush(stdin)(stdout) = limpia el buffer asì llega un caracter a la memoria RAM

getch = ejecuta no replica.

getche = ejecuta solo al tocar el caracter(sin enter). // SE DEFINE VALOR.

flag para no depender de limitaciones.

(float)variable = castea de int a float EL VALOR ACTUAL*/



}
